{% doc %}
  Outputs a Logo with a heading
{% enddoc %}

<div class="logo-heading">
<{{ block.settings.size }}><a href="#"> {{ block.settings.text }} </a> </{{ block.settings.size }}>
</div>

{% stylesheet %}
  .logo-heading {
    display: flex;
  }
{% endstylesheet %}

{% schema %}
{
    "name": "Logo Heading",
    "settings": [
        {
            "type": "text",
            "id": "logo_text",
            "label": "Text",
            "default": "Text"
        },
        {
            "type": "select",
            "id": "size",
            "label": "Size",
            "options": [
                {
                    "value": "h1",
                    "label": "Heading 1"
                },
                {
                    "value": "h2",
                    "label": "Heading 2"
                }
            ]
        }
    ]
}
{% endschema %}